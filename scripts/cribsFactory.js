//refering to the module which is in app.js
//angular
	//.module('ngCribs')
	//.factory('cribsFactory', function ($http) {
		/* body... */
//below is the local array that we are making(change: now data comes from from data.json)
		//var cribsData=

		//we need a function to return this data
	//	function getCribs() {
			/* body... */
			//return cribsData;
	//		return $http({
      //method: 'GET',
      //url: 'data/data.json'
   //});
	//	}
	//	return {
	//		getCribs: getCribs  //references the above function
	//	}
	//})
	//This does not work with latest version of angular